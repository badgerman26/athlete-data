name: Coach Tony Master Sync
on:
  schedule:
    - cron: 
    - cron: '0 8 * * *'    # 8 AM UTC Morning Wellness
  workflow_dispatch:

jobs:
  sync-and-audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install pandas requests

      - name: Run Master Sync
        env:
          INTERVALS_ID: ${{ secrets.INTERVALS_ID }}
          INTERVALS_API_KEY: ${{ secrets.INTERVALS_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python sync.py

      - name: Commit and Push Data
        run: |
          git config --global user.name "Coach Tony Bot"
          git config --global user.email "bot@coachtony.com"
          git add activities.csv wellness.csv latest_report.txt
          git commit -m "Coach Tony Data Sync [skip ci]" || echo "No changes"
          git push

      - name: Send Email
        if: success()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Message from Coach Tony"
          to: robbie.whittaker@gmail.com
          from: "Coach Tony"
          body: file://latest_report.txt
